<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.dartx.wordcards">

    <application
        android:name=".activities.MainApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_icon_100"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_icon_100"
        android:supportsRtl="true"
        android:theme="@style/Theme.WordCards">
        <activity
            android:name=".activities.CardActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".activities.DelayDialogActivity"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:taskAffinity=""
            android:theme="@style/Theme.AppCompat.Dialog" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>